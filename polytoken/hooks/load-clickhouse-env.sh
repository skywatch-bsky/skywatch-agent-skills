#!/usr/bin/env bash
# Polytoken pre_tool_use hook placeholder.
# Current Polytoken pre_tool_use hooks can allow/deny but cannot modify shell_exec
# input, so environment loading is done with `direnv exec . <command>` instead.
exit 0
